import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class App extends Component {
    render() {
        return "React app jega"
    }
}

ReactDOM.render( < App / > , document.getElementById('app'));